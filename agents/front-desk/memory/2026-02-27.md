# 🎤 语音交互优化配置 - 2026-02-27

## 问题发现
- **原始问题**：使用 OpenClaw 的 `tts` 工具无法生成可播放音频（返回的是 ElevenLabs 音频链接）
- **根本原因**：Agent 应该调用本地 Coqui TTS API，而不是用系统级 tts 工具

## 解决方案

### ✅ 正确流程（优先级最高）

1. **调用 Coqui TTS 本地服务**
```bash
curl -s -X POST http://127.0.0.1:8000/v1/text-to-speech \
  -H "Content-Type: application/json" \
  -d '{"text":"您的回复内容"}'
```

2. **获取返回的 audio_url**
```json
{"status":"success","audio_url":"/Users/cage/llm_tools/coqui_tts/output.wav"}
```

3. **播放音频文件**
```bash
afplay /Users/cage/llm_tools/coqui_tts/output.wav
```

### 🔁 回退机制

如果 Coqui TTS 服务不可用（连接拒绝/超时）：
1. **优先尝试**: macOS `say` 命令作为备用
   ```bash
   say -v "Hello" "您的回复内容"
   ```
2. **失败后**: 直接切换到文字回复，告知用户语音功能暂时不可用

## 配置文件位置
- **Coqui TTS 服务**: `http://127.0.0.1:8000` (已运行且 healthy)
- **技能文档**: `~/work/hotel_bot_app/hotel-front-desk-agent/agents/front-desk/skills/voice-interaction/SKILL.md`
- **输出目录**: `/Users/cage/llm_tools/coqui_tts/`

## 测试验证
✅ 2026-02-27 14:24 - 服务状态检查通过
✅ 2026-02-27 14:25 - 成功生成并播放 WAV 文件
⚠️ 2026-02-27 14:31 - Coqui TTS API 响应超时
⚠️ 2026-02-27 15:09 - Coqui TTS API 返回 404 Not Found，服务不可用

## 使用说明

在 Agent 对话中，当用户请求语音回复或系统处于 Talk Mode 时：

```
用户说："用语音回答我"
↓
Agent 执行：
1. exec "curl -s -X POST http://127.0.0.1:8000/v1/text-to-speech ..."
2. 提取 audio_url 字段
3. afplay $audio_url &
4. 等待播放完成
↓
如果失败 → 直接用文字回复并说明情况
```

---
*记录时间：2026-02-27 14:26 GMT+8*
*场景：酒店前台 Agent (小瑞) 语音功能优化*
